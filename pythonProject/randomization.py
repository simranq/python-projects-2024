#we can also import our other python files and access it's variables.
import random

random_int = random.randint(1,10)
print(random_int)

random_float = random.random() #it generates a random floating no. b/w 0 & 1
print(random_float)

# a = random_float * 5   this will pick a random floating no. b/w 1-5

